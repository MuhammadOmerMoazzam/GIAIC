// storing the name of the famous person in a variable
let famous_person: string = "Elon Musk";

// composing the message
let message: string = `${famous_person} once said, \"A person will panic to find a charger before their phone dies, but won't panic to find a plan before their dream dies.\" `;

// display the message
console.log(message);